These are some data structures I am imlementing in C++.
Currently has:
Singly linked list
Stack using vector

To build:
```
mkdir build
cd build
cmake ..
```

To build for Xcode:
```
mkdir build
cd build 
cmake .. -G Xcode
```
